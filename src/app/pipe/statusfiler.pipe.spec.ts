import { StatusfilerPipe } from './statusfiler.pipe';

describe('StatusfilerPipe', () => {
  it('create an instance', () => {
    const pipe = new StatusfilerPipe();
    expect(pipe).toBeTruthy();
  });
});
