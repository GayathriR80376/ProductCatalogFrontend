import { AdminNameFilterPipe } from './admin-name-filter.pipe';

describe('AdminNameFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new AdminNameFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
