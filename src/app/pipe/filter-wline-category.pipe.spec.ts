import { FilterWlineCategoryPipe } from './filter-wline-category.pipe';

describe('FilterWlineCategoryPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterWlineCategoryPipe();
    expect(pipe).toBeTruthy();
  });
});
